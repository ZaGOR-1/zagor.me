# Quick Start Guide - Оптимізований Портфоліо Сайт

## 🚀 Швидкий Старт

### Встановлення

```bash
# Клонування репозиторію
git clone https://github.com/ZaGOR-1/testanyproject.git
cd testanyproject

# Встановлення залежностей
npm install

# Запуск dev сервера
npm run dev
```

Сайт буде доступний за адресою: http://localhost:3000

---

## 📦 Основні Команди

```bash
# Development
npm run dev          # Запустити dev сервер

# Production
npm run build        # Зібрати для продакшн
npm run start        # Запустити продакшн сервер

# Quality Checks
npm run lint         # Запустити ESLint

# Export
npm run export       # Експорт статичного сайту (те саме що build)
```

---

## 🏗️ Структура Проєкту

```
testanyproject/
├── app/                      # Next.js App Router
│   ├── [locale]/            # Локалізовані сторінки
│   │   ├── layout.tsx       # Layout для локалі
│   │   └── page.tsx         # Головна сторінка
│   ├── layout.tsx           # Root layout
│   ├── page.tsx             # Root redirect
│   └── globals.css          # Глобальні стилі
│
├── components/              # React компоненти
│   ├── Navbar.tsx          # Навігація
│   ├── HeroSection.tsx     # Головна секція
│   ├── AboutSection.tsx    # Про мене
│   ├── SkillsSection.tsx   # Навички
│   ├── EducationSection.tsx # Освіта
│   ├── ProjectsSection.tsx # Проєкти
│   ├── ContactForm.tsx     # Форма контактів
│   ├── Footer.tsx          # Футер
│   ├── ScrollProgress.tsx  # Прогрес скролу
│   ├── ErrorBoundary.tsx   # Обробка помилок
│   └── ...                 # Інші компоненти
│
├── lib/                     # Утиліти
│   └── i18n.ts             # Конфігурація i18n
│
├── hooks/                   # Custom React hooks
│   ├── useReducedMotion.ts
│   └── useIntersectionObserver.ts
│
├── types/                   # TypeScript типи
│   └── index.ts
│
├── locales/                 # Переклади
│   ├── uk/translation.json # Українська
│   └── en/translation.json # Англійська
│
└── public/                  # Статичні файли
    ├── robots.txt
    ├── sitemap.xml
    ├── manifest.json
    └── favicon/            # Іконки
```

---

## 🎨 Редагування Контенту

### Зміна Текстів

Редагуйте файли перекладів:
- `locales/uk/translation.json` - Українська
- `locales/en/translation.json` - Англійська

### Зміна Навичок

В `components/SkillsSection.tsx`:

```typescript
const skills = {
  frontend: [
    { name: 'React', level: 85 },
    { name: 'Next.js', level: 80 },
    // Додайте більше...
  ],
  // Інші категорії...
};
```

### Додавання Проєктів

В файлі перекладів (`locales/uk/translation.json`):

```json
{
  "projects": {
    "items": [
      {
        "title": "Назва проєкту",
        "description": "Опис проєкту",
        "technologies": ["React", "Node.js"],
        "github": "https://github.com/...",
        "demo": "https://demo.com"
      }
    ]
  }
}
```

---

## 🎨 Налаштування Стилів

### Кольори

В `tailwind.config.js`:

```javascript
colors: {
  primary: {
    50: '#f0f9ff',
    // ... інші відтінки
    900: '#0c4a6e',
  },
}
```

### Темна Тема

Використовуйте `dark:` класи Tailwind:

```jsx
<div className="bg-white dark:bg-gray-900">
  <p className="text-gray-900 dark:text-gray-100">Текст</p>
</div>
```

---

## 🌐 Деплой

### Vercel (Рекомендовано)

1. Push код на GitHub
2. Імпортуйте проєкт на [Vercel](https://vercel.com)
3. Vercel автоматично налаштує збірку
4. Сайт буде автоматично деплоїтися при кожному push

### GitHub Pages

1. Налаштуйте GitHub Pages в налаштуваннях репозиторію
2. Виберіть Source: GitHub Actions
3. Push на `main` branch
4. Сайт буде доступний за адресою: `https://yourusername.github.io/testanyproject/`

### Інші Платформи

Для інших платформ:
```bash
npm run build
```

Папка `out/` містить статичні файли готові до деплою.

---

## 🔧 Налаштування

### Зміна URL

В `app/layout.tsx` і `next.config.js`:

```typescript
metadataBase: new URL('https://your-domain.com')
```

### Додавання Аналітики

В `app/layout.tsx` додайте скрипт:

```jsx
<script
  defer
  data-domain="your-domain.com"
  src="https://plausible.io/js/script.js"
/>
```

### Зміна Мов

В `lib/i18n.ts`:

```typescript
export const locales = ['uk', 'en', 'pl'] as const; // Додати нову мову
```

Створіть новий файл: `locales/pl/translation.json`

---

## 🐛 Вирішення Проблем

### Помилка Build

```bash
# Очистити кеш
rm -rf .next node_modules
npm install
npm run build
```

### TypeScript Помилки

```bash
# Перевірити типи
npx tsc --noEmit
```

### Стилі Не Застосовуються

```bash
# Перезапустити dev сервер
# Ctrl+C для зупинки
npm run dev
```

---

## 📊 Перевірка Продуктивності

### Lighthouse

```bash
npm install -g lighthouse
npm run build
npm run start
lighthouse http://localhost:3000 --view
```

### Локально

1. Відкрийте Chrome DevTools
2. Перейдіть до вкладки "Lighthouse"
3. Натисніть "Analyze page load"

---

## 🔐 Безпека

Всі security headers налаштовані в `vercel.json`:
- X-Content-Type-Options
- X-Frame-Options
- X-XSS-Protection
- Referrer-Policy
- Permissions-Policy

---

## 📱 PWA

Сайт має базову PWA підтримку:
- `public/manifest.json` - Маніфест додатку
- Іконки в `public/favicon/`
- Theme color налаштований

Для повної PWA потрібен Service Worker (майбутнє покращення).

---

## 🎯 Корисні Поради

1. **Git Workflow**
   ```bash
   git checkout -b feature/my-feature
   # Внесіть зміни
   git add .
   git commit -m "feat: опис змін"
   git push origin feature/my-feature
   ```

2. **Тестування на Мобільному**
   - Використовуйте Chrome DevTools Device Mode
   - Або відкрийте на реальному пристрої: `http://YOUR_IP:3000`

3. **Оптимізація Зображень**
   - Використовуйте WebP формат
   - Стискайте зображення перед додаванням
   - Розмір < 500KB для кожного

4. **SEO**
   - Оновіть `sitemap.xml` при додаванні нових сторінок
   - Перевірте `robots.txt`
   - Налаштуйте Open Graph зображення

---

## 📚 Документація

- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Framer Motion](https://www.framer.com/motion/)
- [TypeScript](https://www.typescriptlang.org/docs/)

---

## 🤝 Контрибуція

1. Fork репозиторій
2. Створіть feature branch
3. Commit зміни
4. Push до branch
5. Створіть Pull Request

---

## 📞 Підтримка

При виникненні проблем:
1. Перевірте `OPTIMIZATIONS.md` для деталей
2. Перегляньте `IMPROVEMENTS_SUMMARY.md`
3. Перевірте документацію Next.js
4. Створіть Issue на GitHub

---

## ✅ Чеклист Перед Деплоєм

- [ ] Оновити особисту інформацію в перекладах
- [ ] Змінити email адресу
- [ ] Оновити соціальні посилання
- [ ] Додати реальні проєкти
- [ ] Перевірити всі посилання
- [ ] Протестувати на мобільному
- [ ] Запустити Lighthouse аудит
- [ ] Оновити README.md
- [ ] Перевірити meta теги
- [ ] Оновити sitemap.xml

---

**Готово до запуску!** 🎉

Успіхів з вашим портфоліо! 🚀
